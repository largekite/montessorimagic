// Generated by Skylia AI Builder — MontessoriMagic
// DO NOT EDIT MANUALLY — regenerate via the Skylia dashboard

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}


// Platform users synced from Clerk
model User {
  id         String   @id @default(cuid())
  clerkId  String @unique
  email  String @unique
  name  String?
  avatarUrl  String?
  stripeCustomerId  String? @unique
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  subscriptionId  String?
  subscriptionStatus  String?
  currentPeriodEnd  DateTime?
  workspacemembers  WorkspaceMember[]
  childprofiles  ChildProfile[]
}

// Family workspace — top-level tenant
model Workspace {
  id         String   @id @default(cuid())
  name  String
  slug  String @unique
  plan  String @default("free")
  logoUrl  String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  workspacemembers  WorkspaceMember[]
  subscriptions  Subscription[]
  childprofiles  ChildProfile[]
}

// User membership in a workspace with role
model WorkspaceMember {
  id         String   @id @default(cuid())
  workspaceId  String
  userId  String
  role  String @default("member")
  joinedAt  DateTime @default(now())
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  workspace  Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  user  User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// Stripe subscription per workspace
model Subscription {
  id         String   @id @default(cuid())
  workspaceId  String
  stripeSubscriptionId  String @unique
  stripePriceId  String
  status  String
  seatCount  Int @default(1)
  currentPeriodEnd  DateTime
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  workspace  Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
}

// Individual child profile with age and learning preferences
model ChildProfile {
  id         String   @id @default(cuid())
  workspaceId  String
  createdById  String
  name  String
  dateOfBirth  DateTime
  interests  Json?
  learningGoals  Json?
  avatarUrl  String?
  readingStreak  Int @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  workspace  Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  books  Book[]
}

// AI-generated personalized Montessori book for a child
model Book {
  id         String   @id @default(cuid())
  workspaceId  String
  childProfileId  String
  title  String
  subject  String
  ageGroup  String
  content  Json
  coverImageUrl  String?
  status  String @default("draft")
  pageCount  Int @default(0)
  readCount  Int @default(0)
  lastReadAt  DateTime?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  childprofile  ChildProfile @relation(fields: [childProfileId], references: [id], onDelete: Cascade)
  activitys  Activity[]
}

// Hands-on Montessori activity linked to a book
model Activity {
  id         String   @id @default(cuid())
  bookId  String
  title  String
  description  String
  materials  Json?
  ageGroup  String
  skillArea  String
  completed  Boolean @default(false)
  completedAt  DateTime?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  book  Book @relation(fields: [bookId], references: [id], onDelete: Cascade)
}
